<Window x:Class="Sklep.Login.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Login" Height="150" Width="300">

    <Border Padding="10">
        <StackPanel>
            <Grid Margin="0 0 0 10">
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Login:" Margin="0 0 0 10" Grid.Column="0" />
                <TextBox x:Name="LoginTxt" Grid.Column="1"/>
            </Grid>
            <Grid Margin="0 0 0 10">
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Password:" Margin="0 0 0 10" Grid.Column="0" />
                <PasswordBox x:Name="PassBox" Grid.Column="1"/>
            </Grid>
            <Button x:Name="LogujBtn" Click="LoginBtn_Click" Content="Loguj" Margin="95,0,95,0"/>
        </StackPanel>
    </Border>
</Window>
